var context, canvas;var canvas;var context;var ga = 0.0;var timerId = 0;var readyToPressStart = false;  // used to cause a delay in the title screen before user can pres startfunction timeout_before_press_start(delayTime){  delayTime = delayTime * 1000;  // convert to miliseconds  setTimeout( function() {    readyToPressStart = true;  }, delayTime);}function fadeInTitle(){  base_image = new Image();  base_image.src = '/images/ui/title_screen.png';      base_image.onload = function(){    context = titleScreen.canvas;    timerId = setInterval("fadeIn(base_image)", 100);    //titleScreen.canvas.drawImage(base_image, 0, 0);  }}function fadeIn(base_image){  // this call is a little more proper... but it causes flickering  // it's prolly flickering cause I'm creating a new image each time...  context.clearRect(0,0, canvas.width,canvas.height);  context.globalAlpha = ga;    context.drawImage(base_image, 0, 0);    ga = ga + 0.01;  if (ga > 1.0)  {    goingUp = false;    clearInterval(timerId);  }}$(document).ready(function() {  init();	timeout_before_press_start(2);});function init(){  document.getElementById('audio1').play();  canvas = document.getElementById('start_screen'),  context = canvas.getContext('2d');  titleScreen.canvas = context;  fadeInTitle();}// Load all images and stuff here I guess...function load(){  }